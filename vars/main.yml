---
# Role variables

# Cron config files and dirs

cron_files_dirs:
  - /etc/crontab
  - /etc/anacrontab
  - /etc/cron.d
  - /etc/cron.hourly
  - /etc/cron.daily
  - /etc/cron.weekly
  - /etc/cron.monthly
  - /etc/cron.deny
  - /etc/cron.allow
